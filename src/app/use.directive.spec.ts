import { UseDirective } from './use.directive';

describe('UseDirective', () => {
  it('should create an instance', () => {
    const directive = new UseDirective();
    expect(directive).toBeTruthy();
  });
});
